import { BrowserRouter } from "react-router";
import { AppRouting } from "./routing";

function App() {
	return (
		<BrowserRouter>
            <AppRouting />
        </BrowserRouter>
	);
}

export default App;
